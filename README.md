# Portfolio (React + FastAPI)

- **Frontend:** `frontend/` — Create React App + CRACO. Local dev: `npm install` then `npm start`.
- **Backend:** `backend/` — FastAPI + MongoDB + contact email. See `backend/requirements.txt`.

**Deployment:** see [DEPLOY.md](./DEPLOY.md) for environment variables, hosting options, DNS (e.g. Hostinger), and production checks.
